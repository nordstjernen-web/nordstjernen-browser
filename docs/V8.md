# V8 JavaScript engine (experimental backend)

Nordstjernen binds its DOM to one of two JavaScript engines, chosen at
build time:

```sh
meson setup builddir                                        # default: quickjs
meson setup builddir -Djs_engine=quickjs                    # explicit quickjs
meson setup builddir -Djs_engine=v8 -Dv8_root=~/dev/v8-15.2 # experimental V8
```

Both engines implement the same seam: the `ns_js_*` contract declared in
`src/js.h`. Nothing outside the js layer sees an engine type — the
embedder (`src/libnordstjernen.c`, `src/headless.c`) drives page scripting
entirely through that C API, so swapping the engine swaps the whole
implementation behind one header.

## quickjs (default)

The in-tree QuickJS binding is `src/js.c` plus its satellites
(`js_canvas.c`, `js_date.c`, `js_intl.c`, `js_perf.c`, `js_realm.c`,
`ext.c`, `idb.c`, `wasm.c`, `webgl.c`, `webgpu.c`). It carries the full
Web API surface Nordstjernen has: live DOM bindings, events, canvas,
WebCrypto marshalling, workers, MSE, IndexedDB, WebAssembly, WebGL/WebGPU
and the WebExtensions host. The engine itself is the forked quickjs-ng
tree at `src/quickjs/`, built into every configuration.

## v8 (experimental)

`-Djs_engine=v8` swaps all of the above out and compiles `src/js_v8.cc`,
a single C++ file implementing the same `js.h` contract over the V8
embedder API. V8 is never vendored: the build needs a pkg-config `v8`, or
`-Dv8_root` pointing at an extracted monolith build with `include/v8.h`
and `libv8_monolith.a` — the prebuilt releases at
<https://github.com/just-js/v8> work directly. Those releases use LLVM
CREL relocations, so link with lld 19 or newer (`CC_LD=lld CXX_LD=lld
meson setup …`); an older lld or GNU ld leaves every static initializer
unrelocated and the binary crashes in `call_init` before `main`. The
releases are built with pointer compression in a shared cage, so the
build passes `-DV8_COMPRESS_POINTERS` and
`-DV8_COMPRESS_POINTERS_IN_SHARED_CAGE` to `src/js_v8.cc`; V8 aborts at
`V8::Initialize` with an "embedder-vs-V8 build configuration mismatch"
if the embedder and the library disagree. The monolith's objects also
use local-exec TLS and cannot enter a shared library, so this
configuration skips `libnordstjernen.so` and builds the static engine
and executables only.

What the V8 backend does today:

- Full modern-JS execution on V8 15 (the language itself, Promises,
  microtask ordering) for inline and external classic scripts, fetched
  through the browser's own network stack (cookie jar, HSTS, cache).
- **Live DOM bindings** over the same `ns_node` tree the renderer lays
  out: element wrappers cached through the `js_wrapper`/`js_invalidate`
  node hooks, `getElementById` (doc-index backed), `querySelector*`/
  `matches`/`closest` over the shared CSS selector engine,
  `createElement`/`createTextNode`/`createDocumentFragment`, tree
  traversal and mutation (`appendChild`/`insertBefore`/`removeChild`/
  `replaceChild`/`remove`/`cloneNode`), attributes with id/class
  doc-index maintenance, `classList` and a `style` proxy,
  `innerHTML`/`outerHTML`/`textContent` over the shared lexbor fragment
  parser and serializer, and per-node event listeners plus `on*`
  attribute handlers with bubbling, `preventDefault` and
  `stopPropagation`. Browser input (click/key/mouse) dispatches into
  JS, checkbox/radio activation fires `input`/`change`, and every
  mutation flows back into the relayout pipeline, so V8-driven pages
  re-render live.
- `console.*`, timers on the GLib main loop, `requestAnimationFrame`
  driven by the render tick, `queueMicrotask`, `atob`/`btoa`,
  `location` (navigation routes through the embedder's navigate
  callback), `navigator`, `performance.now`, in-memory
  `localStorage`/`sessionStorage`, `matchMedia`/`getComputedStyle`
  stubs, window/document event listeners with correct
  `DOMContentLoaded` → async scripts → `load` → `pageshow` ordering,
  uncaught-exception and unhandled-rejection reporting with stacks.

ES modules run natively on V8's module machinery: external and inline
`type=module` scripts, static imports resolved against the referrer
URL and fetched through the browser network stack (cached per URL),
top-level await, dynamic `import()` and `import.meta.url`.

Dedicated Web Workers run each on their own thread and V8 isolate:
`new Worker(url)` fetches through the browser network stack,
`postMessage` crosses in both directions as structured JSON (with
`onmessage` events on either side), and the worker global has
`self`, `console` (routed to the page log), `importScripts`,
`setTimeout`/`clearTimeout` and `close()`; `terminate()` and page
teardown join the thread cleanly.

`localStorage` persists to disk in the same per-origin GKeyFile
format the QuickJS binding uses, so the two engines share stored data
across sessions (private mode and null origins stay in-memory).

The WebAssembly JS API is V8's own native implementation — `compile`/
`instantiate`/`Module`/`Instance`/`Memory`/`Table` plus JS-shimmed
`compileStreaming`/`instantiateStreaming` over `Response.arrayBuffer`.
A periodic foreground-task pump drains V8's background-thread
completions (async Wasm compile, and any other background task) into
the GLib main loop.

`crypto.subtle` is complete for the common surface — generateKey,
importKey/exportKey (raw/spki/pkcs8), sign/verify, encrypt/decrypt,
deriveBits/deriveKey — marshalled to the same OpenSSL primitives the
QuickJS binding uses (HMAC, AES-GCM/CBC/CTR, RSA, ECDSA/ECDH,
PBKDF2/HKDF). JWK import/export is the remaining format.

Not there yet (next phases below): JWK crypto keys, WebSocket/SSE,
shared/module workers; WebGL, WebGPU, IndexedDB and WebExtensions are
stubbed out of the build.

`about:nordstjernen` reports which engine a binary carries
("QuickJS 0.16.1" vs "V8 15.2.x (experimental backend)").

## Roadmap to engine parity

The goal is for QuickJS and V8 to become equal engines behind the
`js.h` seam. The work is phased; each phase lands as a working,
committed increment:

1. **Core DOM bindings** *(done)* — live element wrappers over
   `ns_node` (cached via `js_wrapper`/`js_invalidate`), tree traversal
   and mutation, attributes/`classList`/`style`, `innerHTML`/
   `textContent` over the shared lexbor fragment parser and serializer,
   `querySelector*` over the shared CSS selector engine, per-node event
   listeners plus browser-dispatched events (click/key/mouse) with
   bubbling and `preventDefault`, and the mutation→relayout wiring.
2. **Forms & interaction** *(done)* — live `value`/`checked` state on
   the shared `data-nd-*` dirty-state convention (`ns_input_used_value`
   / `ns_input_is_checked` / `ns_node_set_editable_value`), select and
   option activation (choose/toggle/step/edge/typeahead) firing
   `input`/`change`, checkbox/radio activation with `:checked`
   restyling, `details`/`summary` toggling with `beforetoggle`/`toggle`
   events, dialog close, form reset, and sequential focus traversal
   over `ns_node_is_focusable`.
3. **Window plumbing** *(done)* — `getBoundingClientRect`/`offset*`/
   `client*`/`scroll*` metrics from the real layout tree (with the
   layout-flush re-entrancy guard), generic `getComputedStyle` over the
   shared style table (`ns_css_prop_id` + `ns_css_value_serialize`),
   real `innerWidth`/`innerHeight`/`screen` from the CSS viewport,
   live `scrollX`/`scrollY`, and a `history` state shim.
4. **Async web APIs** *(done)* — `fetch` (Response/Headers/Request),
   `XMLHttpRequest` (async + sync), `AbortController`/`AbortSignal`,
   `TextEncoder`/`TextDecoder`, and the `Image`/`Option` element
   constructors, all fetching through the browser's async network
   stack (cookie jar, HSTS, cache, cancellation on teardown) with a
   basic same-origin/CORS-header check. wikipedia.org now runs its
   portal script to completion with **zero errors** under V8 and
   renders identically to the QuickJS build. Still ahead in this
   area: persistent `localStorage` (sqlite), streaming response
   bodies, full CORS preflight, the timer nesting clamp and the eval
   budget watchdog.
5. **Bigger surfaces** *(in progress)* — Canvas 2D is done: a cairo
   ARGB32 surface per canvas composited by paint.c, with rects, paths,
   transforms, text, `getImageData` and `toDataURL`. Custom elements
   are done at the pragmatic level: `customElements.define`/`get`/
   `whenDefined`, upgrade of parser- and script-created elements via
   prototype adoption, and `connectedCallback`/`disconnectedCallback`/
   `attributeChangedCallback` wiring. Shadow DOM is done:
   `attachShadow`/`shadowRoot` over the engine's
   `data-nd-shadow-root` flattening convention, declarative-root
   adoption included. WebCrypto covers the core:
   `crypto.getRandomValues`/`randomUUID` (OpenSSL RAND) and
   `crypto.subtle.digest` over the shared `ns_crypto_digest`; the
   rest of `subtle.*` (keys, sign/verify, encrypt/decrypt) is still
   open, as are workers. A dedicated **v8 CI workflow** builds this
   configuration with the cached monolith and smokes it headless on
   every push.
6. **Full parity** — media/MSE, WebGL/WebGPU, WebAssembly, the
   WebExtensions host; a CI job that builds and smokes the v8
   configuration.

## Why keep both

The QuickJS binding is ~50k lines whose value is the *binding* work, not
the interpreter: every DOM/Web API is hand-marshalled against QuickJS
types. The V8 backend proves the `js.h` seam is real — a second engine
drops in without touching the embedder — and gives a yardstick for pure
JS-engine behaviour (spec-current language features, error quality,
performance) independent of our bindings. Growing it into a full
alternative would mean porting the binding layer to V8's handle/template
model piece by piece; the seam now exists to do that incrementally.
